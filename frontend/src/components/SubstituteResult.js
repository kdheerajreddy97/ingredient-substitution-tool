import React from 'react';

const SubstituteResult = ({ substitute }) => {
    return (
        <div>
            <p>Substitute Ingredient: <strong>{substitute}</strong></p>
        </div>
    );
};

export default SubstituteResult;
